export enum Qualities {
  low = 'low',
  middle = 'middle',
  high = 'high',
}

export const colors = ['1', '8', '12', '15', '24'];

export enum Steps {
  addImages = 'Add images',
  croppingImages = 'Cropping images',
  downloadImages = 'Download images',
}

export enum TypeCropWords {
  'px',
  '%',
  'aspect',
}
